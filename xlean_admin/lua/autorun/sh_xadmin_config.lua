local config = {}

config.ServerName = "XLean - Développement"

config.Command = "/admin"

config.Warn = "/warn"

config.Staff = {
    ["superadmin"] = true,
    ["admin"] = true,
    ["modérateur"] = true,
}

xadmin_config = config